


/*export default function repopulateTask(e){
    e.preventDefault()
    masterList.forEach((values, keys) => {
        let newTask = document.createElement("div");
        newTask.id = keys;
        newTask.textContent = "testing";
        toDoSection.appendChild(newTask);
    });
}*/